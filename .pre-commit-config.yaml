repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.8  # keep equal to the ruff pin in pyproject.toml [dev]
    hooks:
      - id: ruff
        args: [--fix]
      - id: ruff-format
